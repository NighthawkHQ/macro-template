{
  "name": "macro-template",
  "version": "1.0.0",
  "description": "Template for building Nighthawk macros",
  "type": "module",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build": "node build.mjs",
    "build:examples": "node build.mjs --examples",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@nighthawk.hq/macro-sdk": "^0.1.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.9.0"
  }
}
