{
  "name": "macro-template",
  "version": "1.0.0",
  "description": "Template for building Nighthawk macros",
  "type": "module",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run build"
  },
  "author": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NighthawkHQ/macro-template.git"
  },
  "license": "ISC",
  "dependencies": {
    "@nighthawk.hq/macro-sdk": "^0.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.9.0"
  }
}
